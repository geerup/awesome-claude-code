{
  "name": "maharat-email-html-build",
  "version": "1.0.0",
  "private": true,
  "description": "MJML component library for the Maharat slotted email build. Compiles to the Ortto-slotted table HTML that equals the committed hand-built source of truth.",
  "scripts": {
    "build:base": "mjml base-template.mjml -o ../build/base-template.html",
    "validate": "mjml --validate base-template.mjml"
  },
  "devDependencies": {
    "mjml": "^4.15.3"
  }
}
