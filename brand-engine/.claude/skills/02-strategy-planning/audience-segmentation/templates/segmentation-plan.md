# Segmentation Plan: [campaign_id]

Internal artifact, stream 2. Produces the segments[] field of the strategy-artifact. Sizes
are traceable to data, never invented. No customer-facing copy. No em dashes, Western
numerals only.

- campaign_id: [from the brief filename]
- segmented_by: strategy-lead
- date: [YYYY-MM-DD]
- entry_point: [paid acquisition | owned audience]

---

## 1. Segmentation axes

The cuts chosen for this campaign and why they matter.

- [axis, e.g. engagement state]: [why it matters for the angle and offer]
- [axis, e.g. prior interest]: [why]
- [axis, e.g. recency of last open]: [why]

## 2. Segments

One block per segment. Keep the set small, roughly 3 to 5.

```
name:       <short, human, e.g. lapsed-engaged-nonpayers>
size:       <figure traceable to data | estimate | resolved from live data at send time>
definition: <what defines membership, in plain words>
why:        <why this segment matters for the angle and the offer>
```

```
name:
size:
definition:
why:
```

```
name:
size:
definition:
why:
```

## 3. Sizing notes

- Planning estimates used (from context/01-brand-brief.md): [which, e.g. about 18,000
  non-paying contacts]. These are planning figures, not confirmed send sizes.
- Resolved from live data at send time: [which segments, recorded in the lifecycle-package].

## 4. Open items (live data needed)

- [segment or size that cannot be confirmed from context, and which stream it affects]

No segment size is invented. Where data is not available, the size is flagged here.
