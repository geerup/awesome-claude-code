# Performance Readout

Internal artifact. Owned by analytics-reporter. Every claim carries its evidence. Optimization
moves are proposals for the human gate, never actions taken.

## Envelope

- campaign_id:
- produced_by: analytics-reporter
- stream: 8 monitoring and optimization
- status: draft | qa-passed
- window read: (start date to end date, e.g. 2026-06-01 to 2026-06-14)
- data sources: (e.g. BigQuery, GA4, only approved tools)

## Success metric (restated from the strategy-artifact)

State the success_metric exactly as the strategy-artifact set it. This is the only bar.
If the strategy-artifact has no success_metric, stop and ask. Do not invent one.

- success_metric:
- target value (if the brief set one):

## Results vs the success metric

| Metric | Actual | Target / success_metric | Gap (beat or miss) | Source | Window |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

State the headline result plainly, miss or beat. No softening.

## What works (with evidence)

- Finding:
  - Evidence (number, source, window):
- Finding:
  - Evidence (number, source, window):

## What does not work (with evidence)

- Finding:
  - Evidence (number, source, window):
- Finding:
  - Evidence (number, source, window):

Do not list vanity metrics (impressions, raw reach, follower count) as a result against the
success_metric.

## Supporting cuts (optional, anchored to the success_metric)

Optional. These cuts support the success_metric, they never replace it and never reintroduce
vanity metrics (impressions, raw reach, follower count) as a result. Use them to locate where
the success_metric moves, not to add new goals. Fill only the cuts that aid the read.

Funnel-stage view (success_metric decomposed by stage, for example reach to engagement to
conversion to revenue, named for this campaign's funnel):

| Funnel stage | Value | Source | Window |
|---|---|---|---|
|  |  |  |  |

Per-segment cut (segments drawn from the strategy-artifact, not invented here):

| Segment (from strategy-artifact) | Success_metric value | Source | Window |
|---|---|---|---|
|  |  |  |  |

## Proposed optimizations (human-gate proposals, not actions)

Each move is a proposal. The skill does not pause, shift budget, or change a send on its own.

| Proposed move | Why (tied to the success_metric) | Expected effect | Decision owner |
|---|---|---|---|
|  |  |  | human gate |

## Open items

- Anything unresolved (data gap, tool not yet approved, metric not yet instrumented).
